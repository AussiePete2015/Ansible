[Unit]
Description=RStudio Server

[Service]
Type=forking
ExecStart=/bin/sh /usr/lib/rstudio-server/bin/rserver
ExecStop=/bin/sh /usr/bin/killall -TERM rserver
User={{ ssh_vpn_user }}
KillMode=none
Restart=on-failure

[Install]
WantedBy=multi-user.target
